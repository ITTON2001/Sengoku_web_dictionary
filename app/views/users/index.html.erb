<h1>ユーザー一覧 作成用</h1>

<%= will_paginate @books, :previous_label => ' &lt 前へ', :next_label => '次へ &gt' %>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= user.name %>
    </li>
  <% end %>
</ul>